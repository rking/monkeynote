require 'working-rails/guard'
guard 'sporkminitest' do
  watch %r|crud_controller| do
    %w(
    test/controllers/leaves_controller_test.rb
    test/controllers/tags_controller_test.rb
    )
  end
end
