%h1 Listing leafs

%table
  %tr
    %th Label
    %th Url
    %th
    %th
    %th

  - @leafs.each do |leaf|
    %tr
      %td= leaf.label
      %td= leaf.url
      %td= link_to 'Show', leaf
      %td= link_to 'Edit', edit_leaf_path(leaf)
      %td= link_to 'Destroy', leaf, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Leaf', new_leaf_path
